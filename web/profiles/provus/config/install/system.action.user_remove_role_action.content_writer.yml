langcode: en
status: true
dependencies:
  config:
    - user.role.content_writer
  module:
    - user
id: user_remove_role_action.content_writer
label: 'Remove the Content Writer role from the selected user(s)'
type: user
plugin: user_remove_role_action
configuration:
  rid: content_writer
